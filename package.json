{
  "private": true,
  "name": "sanity-nextjs-landing-pages",
  "version": "1.0.3",
  "scripts": {
    "build": "npm run build-lambda && lerna run build --parallel",
    "build-lambda": "netlify-lambda build lambda",
    "build-web": "(cd web && npm run build)",
    "build-studio": "(cd studio && npm run build)",
    "dev": "lerna run dev --parallel",
    "format": "lerna run format",
    "graphql-deploy": "lerna run graphql-deploy",
    "lint": "lerna run lint",
    "postinstall": "netlify-lambda install && lerna bootstrap",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@sanity/cli": "^0.147.3",
    "lerna": "^3.9.0"
  },
  "dependencies": {
    "country-code-lookup": "0.0.16",
    "dotenv": "^8.2.0",
    "isomorphic-unfetch": "^3.0.0",
    "netlify-lambda": "^1.6.3",
    "next": "^9.1.7",
    "node-fetch": "^2.6.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-jotform-embed": "^1.0.4",
    "react-select": "^3.0.8",
    "stripe": "^8.29.0",
    "us-state-codes": "^1.1.2"
  }
}